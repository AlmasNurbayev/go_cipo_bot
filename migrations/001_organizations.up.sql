CREATE TABLE IF NOT EXISTS organizations (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY  PRIMARY KEY,
  bin TEXT NOT NULL UNIQUE,
  name TEXT NOT NULL,
  hash TEXT
  
);

CREATE INDEX IF NOT EXISTS organizations_idx ON organizations (bin);